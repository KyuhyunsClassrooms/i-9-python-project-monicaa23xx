# 프로젝트 계획서

## 1. 프로젝트 주제

내가 만들 프로그램의 주제:

수술 후 환자 통증(NRS) 기록 및 무통주사(PCA) 부작용 모니터링기

## 2. 문제 상황



내 설명: 수술 후 통증 지수를 기록하고 무통주사 투입한 상태에서 부작용 여부를 분석해 무통주사 투입을 중단할 지 유지할 지 한눈에 제공해주는 프로그램


---

## 3. 입력-처리-출력 구조

### 입력

사용자가 입력하거나 프로그램이 사용할 데이터는 무엇인가?

초기 데이터: 기존에 등록되어 있는 환자 3명의 정보 데이터
- 키보드(input())를 통한 신규 입원 환자 정보: 환자 번호, 수술명, 통증 점수(0~10 정수형), 부작용 증상 문자열

### 처리

입력된 데이터를 어떤 규칙이나 알고리즘으로 처리할 것인가?

- 2차원 리스트 탐색: for 반복문을 사용하여 병동 환자 리스트의 행을 하나씩 추출

복합 조건 판정 알고리즘:

1순위: 환자의 부작용 열(patient[3])이 '없음'이 아니라면 ➡️ [주입 중단 및 부작용 완화 약물 투여 후 재주입] 판단

2순위: 부작용은 없으나 통증 점수 열(patient[2])이 4점 이상이면 ➡️ [추가 주입] 판단

3순위: 그 외의 경우 ➡️ [유지 및 안정] 판단

1) 함수 분해를 통한 역할별 처리 구조
[출력 및 반복 처리] monitor_all_patients(): 프로그램 시작 시 실행되며, 전체 환자 데이터베이스인 2차원 리스트 patients를 for 반복문으로 순회합니다. 가로줄(행) 데이터를 한 명씩 순서대로 추출하여 진단 함수로 전달하는 흐름을 제어합니다.

[진단 및 조건 처리] analyze_patient(patient): 반복문 내에서 추출된 개별 환자의 데이터를 매개변수로 전달받아, 세부 간호 항목(열 데이터)을 기반으로 환자의 안전 상태를 판정하고 맞춤형 지시서 문장(return)을 생성합니다.

[입력 및 누적 처리] register_new_patient(): 키보드(input())를 통해 신규 환자의 정보를 입력받아 정수형 변환(int()) 등 데이터 정제를 거친 후, 1차원 리스트로 묶어 기존 2차원 리스트에 append()로 누적 저장 및 즉시 진단 처리를 수행합니다.

2) 알고리즘 판단 기준에 따른 다중 분기 처리 구조 (경우의 수 분류)
analyze_patient 함수는 환자의 생체 안전을 최우선으로 확보하기 위해 다음과 같이 3가지 경우의 수로 단계를 나누어 복합 조건을 처리합니다.

[경우 1] 부작용이 발생한 경우 (최우선 순위 처리)

판단 조건: 환자의 부작용 열 데이터가 '없음'이 아닐 때 (if patient[3] != "없음")

처리 내용: 통증 점수의 높고 낮음과 관계없이 환자의 안전을 위협하는 약물 부작용(오심, 어지러움 등)이 발견되었으므로, 최우선 간호 원칙에 따라 즉시 [주입 중단 및 부작용 완화 약물 투여 후 재주입] 지시서 문자열을 반환합니다.

[경우 2] 부작용은 없으나 통증이 심한 경우 (2순위 처리)

판단 조건: 부작용은 '없음'이지만, 통증 점수 열 데이터가 4점 이상일 때 (elif patient[2] >= 4)

처리 내용: 부작용은 없어 안전하지만 환자가 조절되지 않는 중등도 이상의 통증을 겪고 있으므로, 무통주사 추가 버튼(Bolus)을 누르거나 추가적인 진통제 처방을 고려하도록 [추가 주입] 지시서 문자열을 반환합니다.

[경우 3] 통증과 부작용이 모두 안정적인 경우 (3순위 처리)

판단 조건: 위의 두 가지 위험 상황에 모두 해당하지 않을 때 (else)

처리 내용: 통증 점수가 4점 미만으로 잘 조절되고 있으며 부작용도 없는 완벽한 안녕 상태이므로, 현재 약물 주입 속도를 그대로 유지하도록 [유지] 지시서 문자열을 반환합니다.

데이터 누적: 신규 환자 정보를 1차원 리스트로 묶어 기존 2차원 리스트에 append()로 추가

통증도 4 이상이고 부작용도 있을 때 부작용 먼저 판단하는 것은 간호학적으로 무통주사 중 심한 부작용(오심, 구토, 호흡 이상 등)이 나타날 시 환자의 안전에 즉각적인 위협이 될 수 있기 때문이다. 통증이 조금 있더라도 심하게 부작용이 발견되면 주입을 즉시 중단하거나 부작용 완화 약물을 투여한 후 재주입하는 것이 최우선 간호 원칙이다.

### 출력

프로그램이 최종적으로 무엇을 출력하는가?

1) 기존 병동 환자 전체에 대한 실시간 간호 지시서 출력
출력 타이밍: 프로그램 실행 직후 자동으로 출력

출력 형태: for 반복문을 통해 2차원 리스트 내부의 모든 환자 데이터를 순서대로 한 줄씩 출력

세부 출력 내용 (경우별 상세 내용):

부작용 발생 환자 (P001, P003 등): 환자 번호 및 수술명과 함께 위험 경고 아이콘(🚨), 임상적 판단([주입 중단 및 부작용 완화 약물 투여 후 재주입]), 구체적인 간호 중재 지시(부작용 증상 명시, PCA 장금, 주치의 보고 및 항구토제/완화제 투여 필요)가 결합된 형태의 통합 메시지를 출력합니다.

정상 및 안정 환자 (P002 등): 안전 아이콘(✅), 임상적 판단([유지]), 현재 통증과 부작용 상태가 모두 안정적임을 알리는 메시지를 깨끗하게 출력합니다.

2) 신규 환자 등록 처리 결과 및 맞춤형 진단 출력
출력 타이밍: 사용자가 키보드로 새 환자 정보를 모두 입력한 직후 출력

출력 형태: 데이터 접수 완료 알림과 해당 환자만을 위한 독립된 진단 결과 메시지 출력

세부 출력 내용:

데이터 누적 성공 메시지: 입력받은 환자 번호를 활용하여 "[등록 완료] X번 환자가 병동 데이터에 추가되었습니다."라는 안내 문구를 출력하여 데이터베이스(2차원 리스트) 반영 여부를 시각적으로 확인시켜 줍니다.

새 환자 실시간 분석 결과: 새로 등록된 환자의 데이터(예: 통증 점수 5점, 부작용 없음)를 분석 함수에 대입하여, 주의 아이콘(⚠️), 임상적 판단([추가 주입]), 통증 수치 점수 명시 및 추가 주입 고려 지시서 문장을 최종 출력합니다.
---

## 4. 2차원 리스트 설계

내 프로그램에서 사용할 2차원 리스트의 이름:
patients

```python
patients = [
    ["P001", "복강경수술", 6, "오심"],
    ["P002", "충수절제술", 2, "없음"],
    ["P003", "제왕절개술", 3, "어지러움"]
]
```

### 한 행의 의미
행(Row): 입원한 환자 1명의 독립된 정보 가로줄 (가상 환자 데이터 활용)

### 각 열의 의미

열(Column): 환자의 세부 간호 데이터 세로줄 (총 4개 열)

[0] 열: 환자 번호 (예: "P001")

[1] 열: 수술명 (예: "복강경수술")

[2] 열: 통증 점수 (0~10 범위의 정수 데이터)

[3] 열: 부작용 증상 (예: "오심", "어지러움", "없음")

---

## 5. 함수 설계

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
|---|---|---|---|
| analyze_patient | 환자 1명의 상태를 진단하여 알맞은 간호 지시 문장을 생성함 | patient |분석 결과 메시지(return 문자열)  |
|monitor_all_patients  |병동에 있는 전체 환자를 순서대로 확인하며 실시간 상태 분석 결과를 출력함  |없음  | 전체 환자 분석 현황(화면 출력) |
| register_new_patient | 키보드로 새 환자의 정보를 입력받아 기존 환자 데이터 리스트에 반영함 |없음  | 등록 완료 및 새 환자 분석 결과(화면 출력) |
---

## 6. 사용할 제어구조

### 조건문을 사용할 부분


내 계획:환자 데이터 중 부작용 항목이 '없음'이 아니면 즉시 주입 중단 및 부작용 완화 약물 투여 후 재주입을 지시하고, 부작용이 없더라도 통증 점수가 4점 이상이면 추가 주입을 고려하도록 분기하여 처리한다. 모두 해당하지 않는 안정적인 상태일 때는 유지를 선택한다. (함수 analyze_patient 내부에서 활용)

### 반복문을 사용할 부분


내 계획:for 반복문을 사용하여 병동 환자 현황이 저장된 2차원 리스트(patients)에서 환자 데이터를 한 명씩 순서대로 꺼내어 실시간 상태 분석 함수로 전달하고 결과를 출력한다. (함수 monitor_all_patients 내부에서 활용)


---

## 7. 예상되는 예외 상황


1. 신규 환자 등록 시 '통증 점수' 입력란에 0~10 사이의 숫자가 아닌 문자(예: "오점")나 음수를 입력하여 에러가 발생하거나 잘못된 데이터가 저장되는 상황
2. 부작용 증상을 입력할 때 '없음' 대신 실수로 공백(스페이스바)이나 아무것도 입력하지 않고 엔터를 쳐서 조건문 판정(patient[3] != "없음")에 혼선이 생기는 상황

---

## 8. Gemini에게 도움받고 싶은 부분


 문자를 입력했을 때 프로그램이 멈추지 않도록 예외 처리(Try-Except)를 적용하여 코드 안정성을 높이는 방법

사용자가 실수로 공백을 입력하더라도 자동으로 '없음'으로 처리되도록 입력 데이터 정제 방법 자문

2차원 리스트에 저장된 환자 데이터를 파일로 저장하거나 나중에 다시 불러오는 기능 확장 방법
